if not contains "$HOME/Documents/Latest_Codes/Islamic work/Islamic AI Agent" $PATH
    # Prepending path in case a system-installed binary needs to be overridden
    set -x PATH "$HOME/Documents/Latest_Codes/Islamic work/Islamic AI Agent" $PATH
end
